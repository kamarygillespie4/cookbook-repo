<div class="recipes list-group">
  {{#each recipes}}
    <a class="list-item justify-space-between" href="/dashboard/edit/{{id}}">
      <div>{{title}}</div>
      <div>{{format_date createdAt}}</div>
    </a>
      <div>{{ingredients}}</div>
      <div>{{instructions}}</div>
  {{/each}}
</div>